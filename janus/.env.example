# OpenAI API Key (required for Codex planner and fallback)
OPENAI_API_KEY=sk-your-key-here

# Anthropic API Key (optional, for real Claude integration)
# If set, Janus will use Anthropic's official SDK
# ANTHROPIC_API_KEY=sk-ant-your-key-here

# External Claude service (optional)
# If set, Janus will call this HTTP endpoint with POST { task: string }
# CLAUDE_FLOW_URL=http://localhost:3000/plan

# External Claude CLI command (optional)
# If set, Janus will execute this command with the task as an argument
# CLAUDE_FLOW_CMD=claude-flow
